import 'package:flutter/material.dart';
class PromptDetailScreen extends StatelessWidget {
  const PromptDetailScreen({super.key, required this.promptId});
  final int promptId;
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Prompt detail - coming in Task 6')));
}
